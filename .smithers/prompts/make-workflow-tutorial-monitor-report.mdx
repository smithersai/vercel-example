# Narrate build progress

The `create-workflow` child run **{props.childRunId}** is building the user's new workflow.
This user is trying Smithers for the first time — keep your update friendly and concrete.

## Get the current state

Run these commands to see what's happening:
```
bunx smithers-orchestrator inspect {props.childRunId}
```
```
bunx smithers-orchestrator logs {props.childRunId}
```

## Write a progress update

Based on what you find, write **1–3 sentences** telling the user:
- What step `create-workflow` is on right now
  (clarifying the spec → provisioning docs → designing → scaffolding files → verifying graph → documenting)
- Roughly how far along it is
- What comes next

Be encouraging — this is their first workflow build. Make it feel like progress, not a wait.

If the build just finished:
- Celebrate it! Tell them the workflow file was written and point them to `.smithers/workflows/` and `.smithers/ui/`
- Tell them how to run it: `bunx smithers-orchestrator workflow run {props.childRunId}`

{props.schema}
